#!/bin/bash

if devbox_prompt "Update base image?"; then
  devbox_docker pull ubuntu:bionic
fi

devbox_docker build                    \
     --build-arg DEVBOX_UID="$(id -u)" \
     --build-arg DEVBOX_GID="$(id -g)" \
     --tag "$devbox_image_tag"         \
     "$devbox_repo_dir"/docker
