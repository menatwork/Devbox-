#!/bin/bash
workdir="$(devbox_pwd_in_container)"
_exec_docker exec				\
       --interactive				\
       --tty					\
       --workdir "$workdir"			\
       --user www-data				\
       --env SSH_AUTH_SOCK=/tmp/ssh-agent.sock	\
       "$devbox_image_name"			\
       composer "$@"
