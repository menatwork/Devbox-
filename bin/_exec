#!/bin/bash
workdir="$(devbox_pwd_in_container)"
devbox_docker exec				\
       --interactive				\
       --tty					\
       --workdir /"$workdir"			\
       --user www-data				\
       --env SSH_AUTH_SOCK=//tmp/ssh-agent.sock \
       "$devbox_container"			\
       "$@"
