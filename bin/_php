#!/bin/bash
# ! KEEP THE DOUBLE-SLASHES !
# They're required for Windows compatibility.

workdir="$(devbox_pwd_in_container)"
devbox_docker exec		\
       --interactive		\
       --tty			\
       --workdir /"$workdir"	\
       --user www-data		\
       "$devbox_container"	\
       php "$@"
