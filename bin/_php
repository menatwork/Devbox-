#!/bin/bash
workdir="$(devbox_pwd_in_container)"
docker run						\
       --interactive					\
       --tty						\
       --workdir="$workdir"				\
       --user=www-data					\
       --volume="$devbox_projects_dir:/projects"	\
       "$devbox_image_name"				\
       php "$@"
